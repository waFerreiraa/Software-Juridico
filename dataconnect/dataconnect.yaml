specVersion: "v1beta"
serviceId: "jurisolutions"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jurisolutions-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
