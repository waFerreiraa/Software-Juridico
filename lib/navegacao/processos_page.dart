import 'package:flutter/material.dart';

class ProcessosPage extends StatelessWidget {
  const ProcessosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("essa é a pagina de processos"),
      ),
    );
  }
}